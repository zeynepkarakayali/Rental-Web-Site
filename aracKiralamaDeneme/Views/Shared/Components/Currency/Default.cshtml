@model aracKiralamaDeneme.Models.CurrencyRate

@* <div class="card shadow-sm p-2 mb-3 bg-white rounded" style="max-width:220px; max-height: 100px; font-size:0.9rem;">
    <h6 class="card-title text-dark mb-1">💱 Günlük Kur</h6>
    <p class="card-subtitle mb-2 text-dark" style="font-size:0.8rem;">
        <strong>Baz:</strong> @Model?.Base
    </p>

    @if (Model?.Rates != null && Model.Rates.Any())
    {
        <ul class="list-unstyled text-dark mb-0" style="font-size:0.8rem;">
            @foreach (var rate in Model.Rates)
            {
                <li><strong>@rate.Key:</strong> @rate.Value</li>
            }
        </ul>
    }
    else
    {
        <p class="text-muted mb-0" style="font-size:0.8rem;">Kur bilgisi alınamadı.</p>
    }
</div> *@

<span>
    💱 <strong>@Model.Base</strong>
    @if (Model?.Rates != null && Model.Rates.Any())
    {
        @foreach (var rate in Model.Rates)
        {
            <span class="ms-3"><strong>@rate.Key:</strong> @rate.Value</span>
        }
    }
    else
    {
        <span class="ms-3 text-muted">Kur bilgisi alınamadı</span>
    }
</span>
